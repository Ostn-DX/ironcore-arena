---
ticket: AUDIT-0001
title: "Remediation: studio drift detected 2026-02-28"
scope: large
risk: high
allowlist:
  - agents/context/conventions.md
  - agents/context/invariants.md
---

## Goal

Remediate all drift indicators flagged by `studio_audit.py` on 2026-02-28.
This ticket was auto-generated — do not modify the frontmatter manually.

## Drift Summary

- Runs scanned:      10
- Gate failures:     5 (50.0%)
- Vault validation:  FAIL
- Dev gate:          FAIL
- Missing manifests: 2
- Tampered packs:    1
- Repeated files:
  - `agents/context/conventions.md` (7 runs)
  - `agents/context/invariants.md` (6 runs)
  - `agents/context/project_summary.md` (5 runs)

## Acceptance Criteria

- [ ] AC1: Resolve `gate_failure_rate` (current: 50.0, threshold: 30.0)
- [ ] AC2: Resolve `repeated_file_modifications` (current: 7.0, threshold: 5.0)
- [ ] AC3: Resolve `vault_validation_failures` (current: 1.0, threshold: 1.0)
- [ ] AC4: Resolve `missing_manifest_count` (current: 2.0, threshold: 1.0)
- [ ] AC5: Resolve `tampered_pack_count` (current: 1.0, threshold: 1.0)

## Remediation Steps

### 1. gate_failure_rate [CRITICAL]

Gate failure rate 50.0% >= 30% threshold. Open each failing REPORT.md, identify the first failing step, and re-run `python tools/run_ticket.py` after fixing root causes.
### 2. repeated_file_modifications [WARNING]

File(s) modified in 7+ distinct runs: `agents/context/conventions.md` (7x), `agents/context/invariants.md` (6x), `agents/context/project_summary.md` (5x). This may indicate churn — create a focused stabilisation ticket to converge these files to a final state.
### 3. vault_validation_failures [CRITICAL]

Vault validation failed. Run `python tools/validate_vault.py` to list all frontmatter errors in Studio_OS/. Fix each file before any further pipeline runs.
### 4. missing_manifest_count [CRITICAL]

2 run(s) have no context pack manifest — the enforcement pipeline was bypassed. Identify which tickets are affected and re-run them: `python tools/run_ticket.py --ticket <path>`.
### 5. tampered_pack_count [CRITICAL]

1 context pack(s) failed SHA-256 integrity check. Files were modified outside the enforcement pipeline. Rebuild affected packs: `python tools/build_context_pack.py <ticket.md>`.

## Notes

- Auto-generated by `tools/studio_audit.py` on 2026-02-28
- Resolve every violation listed above, then re-run the audit to verify
- Re-run: `python tools/studio_audit.py`

## Definition of Done

`python tools/studio_audit.py` exits 0 (status: CLEAN).
